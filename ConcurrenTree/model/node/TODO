* Add limit to ChildSets
* Add limit to SingleNodes
* Confirm non-clobbering
* Add a value parameter back to Map
  * Initialize SingleNodes in map by value
* Change how list value param works
  * Use value based on input
  * Initialize childsets with limits based on value
